<UserControl xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BusLab"
        x:Class="BusLab.RibbonButton"
        x:DataType="local:RibbonButton"
        mc:Ignorable="d"
        Background="Transparent"
        Width="55" Height="66">

    
    <Button CornerRadius="5" Padding="-2" ToolTip.Tip="{Binding ToolTip}">
        <DockPanel>
            <TextBlock DockPanel.Dock="Bottom" 
            VerticalAlignment="Top" 
            Text="{Binding Caption}" FontSize="11" 
            Height="30"
            HorizontalAlignment="Center" 
            TextAlignment="Center"
            TextWrapping="Wrap"/>

            <Image Height="64" Width="40" Source="{Binding Icon}" Stretch="Uniform"/>
        </DockPanel>
    </Button>

</UserControl>
